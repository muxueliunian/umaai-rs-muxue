/target
/docs
logs
/training_data
*.pyc
*.svg
saved_models
pub
docs
training
scripts/export_support_card/*.json
master.mdb
.claude
.portable

# Generated benchmark and training reports
/benchmark-results/

# Local Go toolchain caches (used for pprof tooling during profiling)
.gocache/
.gopath/
/tmp/

# 运气分重放分析产物（明细/统计 CSV，按需生成）
luck_replay*.csv


# 各轮实验的一次性驱动脚本（根目录 scratch_*）：只在本机跑，不入库。
# 预登记、冻结输入与结果证据都在 logs/ 下（logs/ 本身也不入库）。
scratch_*.sh
scratch_*.py
