# 新手写教师 4096：云端采集任务书

状态：需求已授权；尚未下发、尚未开跑。正式启动前必须完成下述版本整合、配方冻结和冒烟。本文不是已经可直接执行的旧驱动命令。

## 1. 目标与边界

用户批准：每候选 4096 次 rollout，40000 个通用有效根，另加 10000 个地区选择有效根。无一晚/12 小时完成限制；运行至各层有效目标完成，或遇到明确安全/数据错误。不得因耗时长主动降到 1024/2048、缩减配额或以尝试数冒充有效数。

只做代码准备、采集、raw 导出与验收；不训练、不部署、不上传模型到公开渠道、不创建新付费机器、不修改 PR 默认模型。使用用户点名的既有云服务器与工作目录；不知道目标时先询问，不猜连接地址，不扫描网络。

## 2. 上游核实结果与版本冻结

2026-09-14 已实际执行 `git fetch upstream`，master 为 `8a23566`。相关新增提交为 `9ebf105` 与 `8a23566`，不得把远端仍未发布的“明天修复”当作已存在。

已读代码确认：

- `local_ramen_trainer.rs` 的 `RecommendedRamenTrainer::new()` 新增已满主属性训练的 PT 定价。`ramen_pt_sacrifice_score=0` 时，有彩圈定价 36、无彩圈定价 16；未满位继续使用原定价。新增 reserve 等实验入口不等于默认启用了所有实验方案。
- `for_rollout()` 调用 `Self::new()`，仅关闭观测记录。`FlatSearch<RamenGame>` 经 `default_rollout_trainer()` 使用它，因此整合后新逻辑会进入教师续跑，不只是客户端入口。
- 上述差异未改地区选择函数本身；但地区候选选完后的训练行为会变化，所以地区候选估值可能变化。
- `search_score()` 区分普通 `score` 与 PT 偏好 `score_pt`。当前 `flat_search.rs` 有序 raw 写入 `outcome.score.score`；不是 PT 偏好分。新默认 `pt_favor_rate=1.0`，不能根据提交标题宣称 raw 计分口径整体改变。
- 以上是源码接线核实，不是独立复现“新手写均分提升”的统计证明。

本地 HEAD 仍为 `602bff9`，另有大量未提交的地区 PR 与实验改动，新教师尚未在本地完成整合。不得直接编译当前工作树然后宣称是新教师。

准备阶段在隔离的采集源码目录整合上游与既有采集依赖，保留原工作树、个人配置、旧数据与模型。不要把纯 upstream 当成已含本地全部采集工具。报告实际源代码差异和依赖范围；提交、推送仍需用户确认，不能借采集授权提交全部 PR 工作。正式采集使用经确认的固定源码版本，运行中不追踪 master、不热更新二进制或参数。

## 3. 冻结配方

沿用上一批已经确认的定义，不自行扩成全部 267 个马娘：

- 空间：`gen2_v1`，8 马娘、16 卡、4 构成、4288 个合法计划。
- 从 `scripts/collect/formal2048_0914/plans.json` 复用实际计划字段与顺序；运行时与 Rust 枚举逐字段比较。
- 原 250 组合排除清单：`scripts/collect/formal2048_0914/holdout.json`。保持排除，4038 个可采组合；不要重抽留出。这些面板已有历史使用记录，不能再称全新盲测。
- rollout 教师：整合后的新 `RecommendedRamenTrainer::for_rollout()`，固定 `ramen_pt_sacrifice_score=0`、`pt_favor_rate=1.0`，普通终局评分目标。记录全部实际生效参数及原文配置，不能只记录默认值或 commit。
- roll-in 沿用上一批：`ens_R4_g123`，原始模型及旁车逐字节一致；epsilon=0.15。不要擅自换 G2mix、手写或 MCTS roll-in。本批不声称匹配“搜索客户端仅 NN 地区”的完整部署状态分布。
- inherit、seed_base、rf=1.4 及其余不变项逐字段继承旧正式 manifest。显式采用每候选固定 4096、`use_ucb=false`、地区 `all`、有序 rollout/CRN；禁止用客户端 UCB 的同名 search_n 语义代替。
- 每根所有候选共享相同 rollout 世界列。保留失败槽掩码、并列/等价候选与完整候选集；不提前筛掉劣势组合。
- 保持输入 754、policy 234、已有落盘协议；raw 导出保留 `combo_fields`、4096 列分数及有效掩码。不生成训练标签、不混入旧目录。

地区专项 1:3:3 取最近整数，总计 10000；各构成最终都为 12500 根：

| 层 | s1 | s2 | s3 | s4 | 合计 |
|---|---:|---:|---:|---:|---:|
| general | 10000 | 10000 | 10000 | 10000 | 40000 |
| region_y1 | 357 | 357 | 357 | 357 | 1428 |
| region_y2 | 1072 | 1072 | 1071 | 1071 | 4286 |
| region_y3 | 1071 | 1071 | 1072 | 1072 | 4286 |
| 总计 | 12500 | 12500 | 12500 | 12500 | 50000 |

s1–s4 顺序严格继承旧清单。general 沿用原通用阶段抽样，它自然采到的地区根不抵扣专项配额。最终另外报告实际 stage/year 分布，不宣称全批地区恰好 10000。

所有层覆盖马娘与卡组，沿用原轮转及备用逻辑，不按采样快慢/得分筛根。生成新的完整 index 清单，核查本机、云端登记与历史 manifest，不能复用旧预留区间（包括旧清单未使用的备用）。冻结前检查 index 唯一、index%plan_count 的身份映射、留出排除、分层配额与备用充足。冒烟使用独立新号段。

## 4. 必须先适配的驱动

现有 `run_formal_collect.py` 在校验、命令、续跑检查和导出检查多处写死 2048/22800，并把最初 12 小时截止持久化到 run_state。`prepare_gen2_formal.py` 也写死旧配额和旧预留区间。不能只改 manifest，也不能直接复制 README 命令启动。

用最小改动支持新配方，保留旧配方的严格验证与旧任务恢复语义；不得覆盖旧冻结清单。为本批增加明确的无墙钟截止模式，取消本批 collector 的 `--max-seconds` 和驱动总超时强杀。到达有效目标才完成；不能用定时循环重置旧 deadline 绕过校验。

无截止不等于无保护：磁盘不足、分片/manifest 不一致、非法数据、进程异常、身份变化必须停止并报告。有限重试仅用于可恢复的外部故障，禁止无限重启确定性错误或删除孤立分片后盲跑。保留中断时完整分片，恢复不得重复计数；已经采完但未导出的任务应能单独完成导出。

用假进程测试：新4096/50000校验、旧配方兼容、无截止、跨进程恢复、完成目标停止、导出失败恢复、身份变化拒绝。测试不能启动正式采集。

## 5. 正式启动前验收

1. Release 构建采集器、导出器及所需检查入口；核对新手写配置进入 rollout。验证 `new()` 与 `for_rollout()` 的动作行为一致；执行相关新逻辑测试。不要只验证客户端 MCTS。
2. 至少普通根及三个地区年份各有小冒烟，均使用 4096，独立号段，采完 raw 导出。核对地区 turn=2/23/47、候选=10/10/120（每组三格），第三年完整120组合。
3. 核对实际版本、配置、资产字节、raw宽4096、cand_n与valid计数、CSR、slots/legal_mask、combo_fields/index映射、x及有效分数有限；从有效 raw 重算 mean/stdev(n-1)，报告实际误差。无效槽不得当0分计入统计。
4. 用同一计分/配置/计划/世界做小规模新旧手写基线诊断，记录新旧差异和失败率；不调参挑最好结果，不用小样本保证显著提升。若发现机制/计分不兼容或异常大幅退步，停止询问，不能一边报错一边正式跑。
5. 资产基座需反映整合后的配置；旧模型保持原文，不能要求所有新配置也与旧资产包完全相同。冻结并记录哪些原文字段相对上一批改变，恢复时逐字段/逐字节核对。
6. 按实测各层吞吐和磁盘占用估算总需求，包含 bincode、raw、临时导出与日志；时间仅作 ETA，不作停止条件。线程使用实例实际 CPU 配额；多机则中央清单分配互不重叠任务，禁止多机重复跑全清单。

上述通过且固定采集版本已获确认后，在指定既有云机后台启动。日志记录主机/任务分配、实际线程、版本、配置、PID、启动方式、有效目标及退出码。不得停止或覆盖该机其它已有任务。

## 6. 交付与运行边界

先回报准备检查与启动证据，再持续采到 50000 个有效根完成（除明确错误、安全条件或用户叫停）。后台进程由既有 tmux/nohup 等承载，不依赖聊天会话存活。

逐任务导出到新 `.npy` 目录，保留 bincode/manifest/assets。全批验收候选数、有效数、index全局唯一、全部分层配额、空间/配置一致性、原留出零交集。输出 run_state 的 exports 精确路径、日志与汇总。原始数据完整不等于模型会提升，不在此任务中启动训练。

## 7. 强制安全与协作约束

- 先读工作区 AGENT.md（如存在）及其指引，否则读 AGENTS.md；本地 Windows 使用 pwsh。Rust 仅 Release，不执行 cargo fmt。
- 非必要禁止 SHA256，以及任何替代哈希/内容指纹的计算或校验。文件比较用实际字节、配置和结果用实际字段；采用现有显式身份无哈希路径。不得运行会计算内容指纹的测试。正常 Git fetch/rev-parse 用于读取已有提交身份，不额外生成内容指纹。
- 不丢弃用户改动，不覆盖旧分片、旧清单、模型、个人配置；不 reset/clean/rebase 或擅自提交/推送。文档不包含私密连接凭证或用户绝对路径。
- 无新的服务器地址、凭证、目录信息时，返回缺失项；“写好了任务书”不是“已下发”，“进程启动”不是“采集完成”。
