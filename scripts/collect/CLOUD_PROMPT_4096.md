# 发给云agent的prompt

请准备本轮新教师数据采集；先读AGENT.md（如有）/AGENTS.md，然后读
scripts/collect/formal4096_0914/README.md。此README替代旧2048运行命令。

源码仓库明确为 https://github.com/muxueliunian/umaai-rs-muxue.git，
交付分支 codex/collect4096-newteacher；上游来源明确为
https://github.com/xulai1001/umaai-rs.git，已整合至8a23566。
开始前确认本机agent已实际提交推送，并取得其交付的固定提交号。
分支不存在或提交不符就报告，不把旧602bff9当成新教师，不自行追上游其它提交。

保留 /workspace/umaai-rs-muxue，新任务使用
/workspace/umaai-rs-muxue-t4096 隔离树。按README取固定源码，不混入其它工作。
你之前提的源码/驱动/清单准备项已在本分支实现；先核实，不再自行改配方。

用户确认谷歌云旧数据已完整下载用于训练，不用再做备份完整性检查。
可清理准确识别的旧采集数据/传输分卷，保留模型与旁车、元数据、清单、
号段登记、资产参考及实验日志；不得删除工作区根或模糊通配符目标。
报告实际删除路径、释放空间及保留位置。

目标：每候选固定4096，非UCB；4万通用有效根+1万地区专项有效根，
地区年比约1:3:3（1428/4286/4286）。无12小时截止，完成目标才停；
但磁盘不足、数据错误、异常退出必须停止报告，禁止降低预算或换便宜根。
R4 g123 roll-in，epsilon=.15，内部新手写续跑；不得换模型或开启NN rollout。

按README完成以下事项：
1. 核对云端历史与所有预留区间（包括备而未用），确认新正式及冒烟号段无冲突后登记。
2. 复制原R4模型和旁车，准备新LF资产参考；配置来自固定的新源码，禁止覆盖回旧默认配置。
3. Release --locked构建、两套Python测试、完整清单校验，以及普通/三年地区4096小冒烟。
4. raw逐字段验证、有效计数与统计量重算；如需手工验收用verify_raw_export.py。
5. 报告固定HEAD、实际教师参数、线程配额、磁盘计划、冒烟结果和准备好的启动命令。

用户已授权先交付源码、由云端继续验证并运行。以上检查全部通过后，
记录并回报固定版本与启动证据，直接在后台持续采到50000有效根，不再等待本机在线。
若检查未通过则停止报告，不允许以赶时间为由跳过。
不要在本任务训练、部署或建立PR。恢复沿用同一配方，不覆盖旧数据。
禁止计算或校验SHA256及其它替代内容哈希/指纹；用实际字节/字段比较。
任何缺失资产、空间冲突或代码异常直接报告，不伪造通过证据。
