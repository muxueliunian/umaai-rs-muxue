# gen2_v1 正式扩采发布交接（2026-09-14）

本地修复与小冒烟已完成。**尚未启动正式采集、训练或云连接。** 云端使用本次发布的完整提交，
按 `scripts/collect/README.md` 准备原始资产参考目录并运行，不再复用512定价命令。

## 已完成

- 修复整副支援卡（含友人）的角色冲突；旧gen1的525个计划与独立旧规则逐字段、逐顺序一致。
- 显式空间完整字段比对；Y1配额为零时省略新增序列化字段，保留旧配方输入。
- 保存模型/sidecar/游戏基座原文，续跑与合并直接比较字节，不以mtime或大小代替内容。
- 新导出combo_fields[N,7]完整马娘/卡组字段，不计算新FNV组合键；旧路径原语义保留。
- 显式工作清单、有效根目标、同层备用、根间软截止及有界外部终止；manifest替换不先删旧文件。
- 云端正式清单：2048、uniform、22800有效根；四构成通用各5000，地区按400/1200/1200分年且各构成均分。
  马娘/卡组分散采样，250个保守留出组合，45600个含备用index，正式独占区间[20000000,300000000)。

## 本次验证（实际执行，Rust全部Release）

- 采集bin 9项通过；导出bin 6项通过。
- 新空间相关4项、地区配额3项、旧gen1计划dump 1项通过。
- Python驱动与完整字段4项通过：清单/重复任务、完整元组切分、实时日志/退出码、只终止指定进程。
- 最终两个Release bin构建通过；日志`target/gen2_build_verified_0914.txt`。
- 四层2048小冒烟共5个有效根：通用1、Y1两根、Y2一根、Y3一根。
  地区每根候选10/10/120，全部候选2048个有效槽；原始数组有限值、主键、完整卡组字段及列宽核对通过。
- Y1根间1秒软截止后保留1根，续跑补到2根；已完成重复调用不改manifest。
  错误预算、同大小错误模型被拒绝，manifest字节不变；相同模型移到不同路径可通过。
  导出拒绝“同版本/同数量但完整空间字段不同”，拒绝“同模型名称但原文字节不同”的合并。
- 四组真实冒烟导出均可由NpyShard加载；只生成合成单测标签验证读取/切分，未生成正式标签或训练。

测试日志：`target/gen2_tests_verified_0914.txt`、`target/gen2_sampler_final_0914.txt`、
`target/gen2_quota_final_0914.txt`、`target/gen1_final_0914.txt`；
冒烟证据及退出码：`target/gen2_release_smoke_0914/`，汇总`results.json`。
实际接受的冒烟index：19000154、19025933、19030809、19051743、19077509。
整个[19000000,20000000)保留为本机验证号段，不进正式训练或评估。

## 云端接续

1. 拉取本次发布提交，Release构建两个bin。
2. 接收本机`target/gen2_cloud_assets_0914/`原始参考目录（约10MB），它不在Git中，尚未上传。
   缺模型/参考目录时停止；不能自己用当前文件复制一个“参考”来冒充与本机核对。
3. 核对云端号段与磁盘/CPU配额，先独立小冒烟，再按README启动正式驱动。
4. 采完或12小时到期报告有效数、失败、完整分片、raw导出及退出码，不启动训练。

## 边界

- 本机已验证的是Windows；云端Linux构建和小冒烟仍需云agent执行。
- 正式驱动以假进程及组件冒烟验证，未运行整批22800；不能保证12小时完成。
- 旧combo_key与新combo_fields混训需要后续真实计划字段映射，现有默认训练划分明确拒绝混用或静默回落。
- 历史Claude全量lib的reason颜色测试失败未在本次重跑；本次只宣称以上针对性检查通过。
- 历史512定价导出算过FNV组合键，原“全程无哈希”声明已订正，历史数据不补造资产证明。
- 参考资产、冒烟/测试产物在Git忽略目录。八个原有scratch保留，不进入提交。
