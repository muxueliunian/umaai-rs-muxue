# 新教师 4096 数据：历史混合 / 新规则数据 A/B 训练计划

日期：2026-09-16。状态：Codex 已做数据归属盘点；交由 Claude 完成开训前验收、训练和导出。尚未开训，不授权部署、提交、推送、云端操作。

## 1. 实验问题与结论边界

用户希望比较两个数据配方，而不是只拿最新 5 万根训练：

- **A / AllHistory：既有正式模型用过的历史语料并集 + 新批**，304,261 根。
- **B / PostPT：包含吃面 PT 延后修复的新规则语料 + 新批**，131,621 根。
- 对照模型：现有 `saved_models/arms/ens_G2mix_g123.onnx`，只读、不重训、不覆盖。

两组的差集精确为历史旧规则的 172,640 根。这里“全部”指已进入 R4 / R5-20kdata / G2mix 正式训练谱系的去重语料，不是对 `training_data/**` 做 glob 全收。

**必须区分新规则与新教师：**

1. 规则分界是 `69f9355`（吃面 PT / eat_count 延后至 NextTurn）。`2f20806` / `922becb` 不含修复，`37a8da2` 含修复。Codex 已直接检查 Git 祖先关系及该提交实际变更。
2. 新 5 万根的采集提交是 `d6a0b97`，包含上游 `8a23566` 及新的手写训练决策逻辑。旧的 1024 / 2048 数据不是这个最新教师。
3. 因此 B 是“修复后规则、多版本教师混合”，**不是纯最新教师组**。A 刻意保留旧规则标签作为历史混合对照，**不能称为完全兼容组**。
4. 两组样本量、阶段组成、新批占比、覆盖和教师组成不同；这是数据配方整体比较，不是单因素证明旧规则、4096 预算或教师更新单独贡献多少。
5. 新批占总根数：A 16.43%，B 37.99%；训练划分后的实际比例另报。首轮不人为平衡比例，不加第三个训练臂。

## 2. 已核实的数据归属

下列历史根数均以实际 `index.npy` 长度核实，不只看 `meta.json.stats.samples`。历史选入目录的 254,261 个 index 合并后无重复。

| 数据目录 / 批次 | 根数 | 使用列宽 | 采集提交 | A | B |
|---|---:|---:|---|---|---|
| `training_data/npy_v6ck` | 140,555 | 512 | `2f20806` | 是 | 否：旧规则 |
| `training_data/npy_ood_half101` | 25,856 | 512 | `922becb` | 是 | 否：旧规则 |
| `training_data/npy_dagger_c` | 3,118 | 512 | `922becb` | 是 | 否：旧规则 |
| `training_data/npy_dagger_d` | 3,111 | 512 | `922becb` | 是 | 否：旧规则 |
| `training_data/npy_n4096_w1024` | 3,057 | 1024（原采 4096） | `37a8da2` | 是 | 是 |
| `training_data/npy_n1024` | 3,052 | 1024 | `37a8da2` | 是 | 是 |
| `training_data/npy_r2roll` | 23,522 | 1024 | `37a8da2` | 是 | 是 |
| `training_data/npy_r3roll` | 9,186 | 1024 | `37a8da2` | 是 | 是 |
| `training_data/npy_r4roll` | 10,164 | 1024 | `07c4868` | 是 | 是 |
| `training_data/npy_r4roll2` | 9,840 | 1024 | `07c4868` | 是 | 是 |
| gen2 正式 2048，16 个目录（见下） | 22,800 | 2048 | `602bff9` | 是 | 是 |
| 新教师正式 4096，16 个目录（见 §3） | 50,000 | 4096 | `d6a0b97` | 是 | 是 |
| **合计** | | | | **304,261** | **131,621** |

gen2 2048 的实际输入根目录：`target/acceptance_gen2_0914/full/npy/npy/`，16 个已有 `<job>_<stamp>/`；标签在 `training_data/labels_gen2f2048/<同名>/`。逐个列为冻结输入，不把同父目录早期部分抽取副本带进去。

特别说明：G2mix 没有使用两批 r4roll，R5-20kdata 使用过；本轮两组都加入这独立的 20,004 根。r4roll / r4roll2 的 roll-in 均为 R4，**目录名不代表 rollout 改成 NN**。新批也是 R4 roll-in + 新手写 rollout，不是本地正在优化的 GPU NN-rollout 教师。

### 2.1 明确排除，不自动补齐“所有目录”

- `npy_v1..v6`、相应 raw / reduced 重导出：已核实 index 集合包含于 v6ck，不能叠加。
- `npy_ood_half`：13,568 根包含于 half101；只取 half101 的实际已有快照，不假称整个源采集已完成。
- `npy_r2roll_s3052` / `s9000`：实际只有 3,052 / 9,000 根，是 r2roll 子集；两者 `meta.stats.samples` 仍错误保留父批 23,522，不能据此计数。
- `npy_n4096` / `w4096` / `w512`：与选入的 w1024 是同批根的不同预算视图。首轮沿用历史正式配方 w1024，只保留一份；不是宣称多列无用。
- `npy_gen2f1024`：同一批 gen2 2048 的截断对照，不与原 2048 并用。
- `dagger_b` / `dagger_e`：不在本次正式谱系名单。它们与 C / D 分别有 3,116 / 3,107 个同名 index，但 roll-in 不同，**不能把 ID 碰撞误当成状态逐值相同的重复数据**；不在本轮重编号混入。
- `incoming_*`、冒烟、price/perf/诊断根、旧导出暂存副本：不进入训练。

## 3. 新 5 万根的本地交付与盘点边界

用户指定的下载目录在当前会话中给出，文档用 `<下载数据目录>` 代称，避免将用户绝对路径写进项目文档。

### 3.1 本次已实际核实

Codex 使用只读 ZIP / tar 流遍历，没有展开大数组到磁盘，没有计算内容哈希。

- 7 个 ZIP：`newteacher4096_0914-20260916T053118Z-1-{001,002,004,005,006,007,008}.zip`。
- ZIP 内 **npy 分卷 000–179 共 180 个，无缺号、无重号**。按数字顺序流式拼接后，已实际遍历到 tar 结束并读到全部 16 个数据目录，每目录 13 个数组 + `meta.json`。
- 新目录都记录 `export_version=2`、`plan_count=4288`、`search_n=4096`、提交 `d6a0b97`。
- 实际读取小数组：`index / stage / turn / combo_fields / cand_ptr / cand_n`，合计 **50,000 根、932,908 个候选**；所有 cand_n 为 4096。
- 全新 index 唯一 50,000，最小 300001370，最大 724210990；与选入历史 index 的号段分离。组合字段实际去重为 4,038 组。
- 阶段计数 `[27149, 0, 12064, 158, 10629]`。地区专项 10,000 根之外，通用批自然带入 629 根地区。
- 元数据包 `run_state.json` 记录 16 项 completed / exports，正式配方为 4096 / 50000 / uniform / rf1.4 / R4 roll-in / epsilon0.15 / 新教师参数 PT sacrifice=0、pt_favor_rate=1.0。
- 采集汇总记录 accepted=50000、skipped=637。该 skipped 数来自日志记录，未重新解码所有 bincode 逐项核对。

交付边界：ZIP 内 bincode/data 分卷仅发现 000–221；下载目录另有两个同大小的完整命名 `formal4096_data*.tar.gz`（各 9,186,413,569 B）。**本次没有验证两个整包是否逐字节相同或完整，不能称已验收；也不能因为缺 ZIP 003 就断言训练 npy 缺失。** 训练优先用已经读通的 npy 分卷，不为开训额外展开两份 bincode 包，不删除任何原始包。

ZIP 001 内有完整的 `formal4096_meta_logs.tar.gz` 和一个重名变体，选前者；`REASSEMBLE.md` 含 `md5sum`，**不要照着执行哈希命令**。

### 3.2 冻结的新目录名称

安全解包到新目录 `training_data/newteacher4096_0914/`，以 tar 内 `npy/` 层级对应以下名称。目标若已存在，先比实际字段，禁止覆盖不同内容。

```text
general_s1_1789389960987238895
general_s2_1789405022958308254
general_s3_1789418088865285054
general_s4_1789434011058077546
region_y1_s1_1789447035585891727
region_y1_s2_1789448104960685125
region_y1_s3_1789449150543014857
region_y1_s4_1789450203540297282
region_y2_s1_1789451233405397617
region_y2_s2_1789453568028771088
region_y2_s3_1789455867549921617
region_y2_s4_1789458202079348508
region_y3_s1_1789460522780234076
region_y3_s2_1789473800739065484
region_y3_s3_1789488998890033402
region_y3_s4_1789502788677124532
```

### 3.3 Claude 开训前必须补完的验收

本次不是完整 raw 数值验收。Claude 必须在训练前：

1. 安全流式解包 npy 与元数据，拒绝绝对路径、`..` 逃逸及越界链接；只写工作区新目标。先核算剩余磁盘，避免分卷、重组包、解压副本全部重复落盘。
2. 核查全部数组 dtype / shape / 文件长度、CSR 单调且无空根、slots 范围与 legal_mask 并集、stage/turn、各年份完整合法地区组合、combo_fields 与冻结实际计划逐字段一致。
3. 分块检查 x / 有效 scores 有限，valid 计数与 cand_n 一致；从全部有效 raw 重算 mean / stdev(n-1)，报告误差。无效槽不能按 0 分计算。npy 列宽必须真为 4096。
4. 将实际 index 与 run_state 冻结 work indices 对照，核对配额与重复；完整组合字段与原 250 留出清单交集必须为 0，不能仅凭“有 4038 组合”推定排除正确。
5. 检查所有选入历史目录和 label 的 index、列宽、配置、有限性及合法掩码对齐；不计算残留的 SHA/FNV 字段。旧 label 缺新 alignment 字段不自动当失败，但必须直接核对实际数组并抽样重算。
6. 输出 `logs/newteacher4096_ab_0916/data_acceptance.json` 和输入清单（相对路径、实际数量、提交、roll-in / rollout、规则代际、预算、标签配置）。如果本计划计数不符，先解释差异，禁止静默删根凑数。

## 4. 划分：复用现有组合清单，不重抽

统一使用 `target/gen2_train_0914/dev_validation_combos.json` 的 389 个组合；两组所有批次都调用 `split_refs_by_combos`。不走 `stable_split_refs`、`--split-by`、`make_dev_split.py` 的历史哈希重放，也不调用 `_splitmix64`。

组合标准字段是 `[uma, *sorted(deck)]`，不是原卡槽顺序；特征卡槽仍保持真实计划顺序，**不能排序 x 的卡片 token**。

旧目录外挂字段：

- 原 R4 八组直接用已有 `training_data/combo_fields_backfill/npy_<name>.npy`，逐值核对。
- `r4roll` / `r4roll2` 的外挂当前不存在。用 `target/gen2_train_0914/plan_dump.txt` 的真实 525 计划，按 index % 525 建立，复用 `backfill_combo_fields.py`，不反推 combo_key。
- Codex 已只读验证这两批共 20,004 × 6 个卡槽的类型 one-hot 与真实计划一致，0 处不符；旧四批新规则外挂字段也与重建字段逐值相同。Claude 落盘后再检查实际字段及行顺序。

实际数组盘点得到的划分预期：

| 部分 | 总根 | 训练 | 开发验证 |
|---|---:|---:|---:|
| B 的历史部分（六个旧目录 + gen2 2048） | 81,621 | 74,239 | 7,382 |
| 新 4096 批 | 50,000 | 45,379 | 4,621 |
| **B** | **131,621** | **119,618** | **12,003** |
| A 额外旧规则四批 | 172,640 | 158,026 | 14,614 |
| **A** | **304,261** | **277,644** | **26,617** |

新批按 general / Y1 / Y2 / Y3 的训练数分别为 36286 / 1310 / 3881 / 3902，验证数 3714 / 118 / 405 / 384。

最终 250 留出使用 `scripts/collect/formal2048_0914/holdout.json`；它不是开发验证清单，也不是新的盲测面板（已有历史实验使用）。所有实际训练/开发数据均应与它零交集。

## 5. 标签与训练配方

### 5.1 标签：冻结现有实现，不把本轮变成配方实验

- 复用旧标签时须核对：bootstrap_draws=512、bootstrap_seed=20260830、radical_factor=1.4、policy_temperature=1.0、tie_atol=1e-5。
- 新批使用当前 `labels.py` 生成新 sidecar：`training_data/labels_newteacher4096_0914/<job_stamp>/`。保留真实 4096 列及 CRN 对齐，不能把 bootstrap_draws 改成 4096，不能截到 1024。
- **源码实际口径**：policy 是 `scores @ bootstrap_weights` 的普通均值最优概率；cross-fit 动作选择也是留一列均值；rf1.4 作用于三路 value 的 weightedMean。不是“policy 已按 rf1.4 的秩加权目标打标”。本轮保留该既有口径，另作已知限制，不顺手修改。
- 等价候选并列平分、合法 mask、value cross-fit、choice 权重 0 全部保留。
- 两臂共享同一份新标签，不分别随机生成。记录每批标签来源与实际列宽。

### 5.2 学生：两个配方，各三个初始化成员

沿用 `target/arm_G2mix_seed1/run.json` / `scratch_g2mix.sh` 的真实训练设置：

| 项 | 固定值 |
|---|---|
| 结构 | token_dim=96 / heads=4 / encoder_blocks=2 / mlp_width=256 / mlp_blocks=2 |
| 其余结构 | dropout=0.08 / attention=simple / card_slot_embedding=false / factorized_eat_head=false |
| 参数量 | 每成员 671,797；开跑前核对 |
| batch / lr / head_lr / wd | 256 / 5e-4 / 5e-4 / 2e-5 |
| 日程 | cosine / warmup 300 / final factor 0.02 |
| 预算 | `--max-steps 30000 --no-early-stop --checkpoint-steps 30000` |
| seed / split-seed | 20260830 / 20260830（不重新哈希划分） |
| init-seed | 每臂 1 / 2 / 3 |
| 常量 | `--frozen-constants target/dagger_d_seed1/run.json` |
| 抽样 | 每个唯一训练根等权；不 train-repeat、不动作重加权、不另加地区权重 |
| 附加诊断 | 沿用 `--eval-columns 0 384`，只记录，不用于选择 |
| 起点 | 从头训练，不用 R4/G2mix checkpoint 初始化，不用 resume 偷接旧日程 |

**所有结构参数显式传入**，否则 `ModelConfig.for_dataset` 可能因 A/B 训练样本数不同自动改模型。本轮 B 训练根为 119,618，尤其接近 120,000 的既有自动档位边界。

历史冻结常量只由当时训练侧拟合，并与本次复用的组合划分一致；它是两臂共享的实验控制。B 的“新规则”限定监督样本，不声称归一化常量也完全来自新规则。记录新批目标归一化后的分布；若发现编码/量纲错误先停，不因均分变化自动重拟合两套常量。

首轮固定预算比较的是“相同优化步数下哪个配方更好”，不保证两个配方各自达到最优。报告末段 train/validation 趋势；未收敛只能记为预算限制，不擅自给落后臂加训或换 best.pt。真正中断的本次训练可以在严格校验输入、划分和配置相同后恢复。

输出用新名字（已存在时停下来处理，不覆盖）：

```text
target/arm_NT4096_AllHistory_seed{1,2,3}/
target/arm_NT4096_PostPT_seed{1,2,3}/
saved_models/arms/ens_NT4096_AllHistory_g123.onnx
saved_models/arms/ens_NT4096_PostPT_g123.onnx
```

先完成两臂输入加载与少量训练步的独立冒烟，确认 loss 有限、所有阶段/计数/模型配置正确，冒烟产物不作为正式起点。并发度按本机实际显存和负载确定，不停用户其它任务。

## 6. 评估与交付

### 6.1 本轮训练任务必须完成

1. 每臂三个固定 30k 成员，记录真正完成的 optimizer step、训练时间和停止原因。不得误交 15 epoch 消融 checkpoint。
2. 单成员及三成员平均 logit 的 ONNX 导出；value 按既有反归一化后平均规则。输入 754、输出 245、动态 batch、保守 opset/算子不变。
3. PyTorch vs ONNX Runtime 对拍 batch=1/7，并补真实新批局面，逐元素绝对误差 <1e-4；Rust tract 实际加载/推理冒烟。失败时报告，不降低门槛。
4. 两个新集成与 G2mix 在**同一份** B 开发验证引用（12,003 根）上离线比较；再单列同一份新 4096 验证子集（4,621 根）。两臂不能用各自不同构成的 aggregate regret 来直接排名。
5. 报告按阶段 top1 / regret / value MAE；地区再按 Y1/Y2/Y3 分开，另列马娘、构成、阶段样本数和动作覆盖。均值 regret 与 rf1.4 utility regret 如同时提供，明确区分，不能用错名字。
6. 用各模型自己的归一化常数反归一化 value 后，在原始分数量纲比较。三成员集成只构成一个候选，不能用三个成员当三次独立集成实验来估计重训方差。
7. 交付 `logs/newteacher4096_ab_0916/{preregistration.md,report.md,data_acceptance.json}`、输入与划分实际清单、命令、run.json、日志、六个 checkpoint 与两个集成/旁车路径。报告采用新文件名，不能覆盖失败/作废运行的原始日志。

### 6.2 后续闭环验收计划（训练/导出交卷后再确认执行）

离线结果不能证明提升，不据此替换现役模型。建议固定三臂：G2mix、AllHistory、PostPT，先完成下列无搜索基准：

- 主面板：原 gen2 250 留出组合 × 16 个全新世界，三个模型逐局配对，纯网络 + 既有自选比赛守门 + Canonical，完整模拟器阶段而非未接全协议的真实客户端。
- 回归：gen1 全部 525 组合 × 8 世界，预先单列 113101 全马娘，不再事后只挑最差一格。
- 主比较 B−A；A−G2mix / B−G2mix 为次比较。均分、配对差、按组合聚类区间、失败率、成本一起报告；失败局不剔除，子组不事后筛选。
- 事前查世界登记与实际日志，选择未占用的新号段并登记。不重用旧世界，不把换世界后的历史组合面板叫全新盲测。
- 同一份明确冻结的当前模拟规则、资产、因子、执行入口用于三个模型；不改源码追上游，不混用旧二进制。若构建不可追溯，报告并先完成隔离的 release 构建。
- “只用 NN 地区 + 其它阶段同一搜索”的部署面板另列第二阶段：按年份统计选择分歧和耗时。它很有价值，但成本/面板先确认，不与本轮训练暗中捆绑开长跑。

## 7. 强制操作边界

- 先读根目录 AGENT.md；不存在则读 AGENTS.md 及 project_context.md。中文沟通；Windows 用 pwsh。
- 不开 subagent、不自动委派其它 agent；由 Claude 单独完成。
- **不计算 SHA256、MD5、FNV 或替代内容指纹**。旧 meta 里已存在的字段可以读取，但不重算；比较实际数组/字段/字节。尤其跳过 REASSEMBLE 的 md5sum、旧划分哈希重放、会跑指纹的测试。bench 如执行，显式无指纹模式，并检查其它隐式入口。
- 不 cargo fmt；Rust 仅 release。Python 做小规模针对性测试，不借本次任务跑所有 Rust 历史测试。
- 不删除下载包、旧数据、模型或日志；不覆盖用户配置与现役入口；不为本轮修改模拟器、采集器或搜索算法。
- 不修改 GPU 性能优化实验的侧车/缓存/Graph 代码。训练不依赖它们接生产采集。
- 不提交、不推送、不接云端、不重新采集、不部署。保留脏工作树；若现有训练接口足够，就不做顺手重构。
- 数据损坏、计划身份不明、训练/验证交叉或根数不符且无法解释，必须停在正式训练前，报告精确目录和差异。

## 8. 给 Claude 的执行摘要

执行本计划 §3.3–§6.1：先验收新包和冻结 A/B 输入，补两批 r4roll 的真实组合字段，复用已有组合划分，生成新批标签，两个配方各训三个 30k 成员，导出并做同一验证集对照。不要先重写训练器或改标签配方。完成后停在 Codex review 前，交回证据与产物；§6.2 长闭环先等用户确认。

已有数字是核对基准，不是要求程序伪造一致。若发现本计划错误，先指出具体证据并修正实验登记，不能以“按任务书”掩盖错误。
